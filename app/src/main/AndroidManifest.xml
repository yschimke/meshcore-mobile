<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <permission
        android:name="ee.schimke.meshcore.permission.MESH_SERVICE"
        android:protectionLevel="signature"
        android:label="MeshCore Service Access"
        android:description="@string/mesh_service_permission_description" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_START_FOREGROUND_SERVICES_FROM_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MeshcoreApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="MeshCore"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service for persistent BT connection notification -->
        <service
            android:name=".service.MeshcoreConnectionService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- gRPC binder service for inter-app mesh access -->
        <service
            android:name=".service.MeshcoreBinderService"
            android:exported="true"
            android:permission="ee.schimke.meshcore.permission.MESH_SERVICE">
            <intent-filter>
                <action android:name="ee.schimke.meshcore.MESH_SERVICE" />
            </intent-filter>
        </service>

        <!-- gRPC Data Layer service for Wear OS companion app -->
        <service
            android:name=".service.MeshcoreWearDataService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/WearGrpcService/ee.schimke.meshcore.grpc.MeshcoreService" />
            </intent-filter>
        </service>

        <!-- BLE device presence detection via CompanionDeviceManager -->
        <receiver
            android:name=".ble.DevicePresenceReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE">
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceManager.COMPANION_DEVICE_APPEARING" />
            </intent-filter>
        </receiver>

        <!-- App Functions — exposes device status, contacts, messages to system assistant.
             The AppFunctionService action is API 36+; on older devices the service
             is harmlessly never bound. -->
        <service
            android:name="androidx.appfunctions.service.PlatformAppFunctionService"
            android:exported="true"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE"
            tools:targetApi="36">
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <!-- Device Info widget — requires API 37+ (Remote Compose) -->
        <receiver
            android:name=".widget.DeviceInfoWidgetReceiver"
            android:exported="false"
            android:enabled="@bool/widget_enabled">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="ee.schimke.meshcore.app.WIDGET_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_battery_info" />
        </receiver>
    </application>
</manifest>
